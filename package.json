{
  "name": "cloud-ui-llms",
  "private": true,
  "type": "module",
  "description": "Documentation for @cloud/ui. The markdown is the source; the HTML is a view of it.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "npm run build && node scripts/serve.mjs",
    "preview": "npm run serve",
    "preview:install": "npm i --no-save ./cloud-ui-0.0.1.tgz",
    "audit:props": "node scripts/audit-props.mjs"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.2.9",
    "@fontsource-variable/geist-mono": "^5.2.8",
    "marked": "^15.0.7"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.2",
    "esbuild": "^0.28.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwindcss": "^4.3.2"
  }
}
